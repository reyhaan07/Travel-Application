# Treat Excel demo data and PDFs as binary (no diff / no line-ending normalization)
*.xlsx binary
*.xls binary
*.pdf binary

# UiPath workflow and config files are text
*.xaml text
*.json text
*.md text
